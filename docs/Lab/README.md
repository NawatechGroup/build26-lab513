# Introduction - Azure SQL Hyperscale for AI & RAG Workloads

This lab is built on Azure SQL Hyperscale, a cloud-native database architecture designed for high-performance, scalable, and AI-driven applications.

## Why Hyperscale?

Azure SQL Hyperscale separates compute and storage, enabling:

- Independent scaling of compute and storage
- Rapid database growth, up to 100 TB
- High-performance workloads with minimal latency
- Fast database backups and restores

## Key Features for AI Scenarios

This lab leverages Hyperscale capabilities to support modern AI patterns:

- Vector support for semantic search and embeddings
- High throughput for retrieval workloads (RAG)
- Separation of compute and storage for scalable AI pipelines
- Integration with Azure services such as Azure OpenAI, Microsoft Fabric, and Microsoft Foundry Agents

## Why It Matters for This Lab

In this lab, Azure SQL Hyperscale enables:

- Efficient storage of FAQ content and vector embeddings
- Fast execution of semantic search queries
- A scalable backend for Retrieval-Augmented Generation (RAG)

You will use Hyperscale as the foundation for:

- Vector search
- AI grounding
- Agent orchestration
- Analytics with Fabric

In real-world AI applications, Hyperscale is ideal when combining transactional, AI, and analytics workloads.

## Before You Start

If you are running the lab outside the guided environment, complete [Exercise 00: Prepare the Lab Prerequisites](Instructions/exercise-00.md) before starting the hands-on exercises.
